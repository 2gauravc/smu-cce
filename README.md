### Git clone the repo
git clone https://github.com/2gauravc/smu_cce.git
cd smu_cce

### Create and activate an environment
python3 -m venv cce-env
source cce-env/bin/activate

### Install dependencies
pip install -r requirements.txt